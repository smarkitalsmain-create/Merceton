| Module | Screen/Route | Feature | Test Case Steps | Expected Result | Priority | Status(Pass/Fail) | Remarks | Suggestions |
|--------|--------------|---------|-----------------|-----------------|----------|--------------------|---------|-------------|
| app/page.tsx | `/` | Landing page loads | 1. Navigate to `/` in a fresh browser session. | Landing page renders without errors and main content is visible. | P1 |  |  |  |
| app/sign-in/page.tsx | `/sign-in` | Sign-in page loads | 1. Navigate to `/sign-in` in a browser. | Sign-in form and relevant inputs are rendered without errors. | P0 |  |  |  |
| app/sign-up/page.tsx | `/sign-up` | Sign-up page loads | 1. Navigate to `/sign-up` in a browser. | Sign-up form and relevant inputs are rendered without errors. | P0 |  |  |  |
| app/forgot-password/page.tsx | `/forgot-password` | Forgot password page loads | 1. Navigate to `/forgot-password`. | Forgot password UI renders without errors. | P1 |  |  |  |
| app/reset-password/page.tsx | `/reset-password` | Reset password page loads | 1. Navigate to `/reset-password` with a valid reset token (if required). | Reset password UI renders without errors. | P1 |  |  |  |
| app/503/page.tsx | `/503` | Maintenance page loads | 1. Navigate to `/503` directly. | 503/maintenance page renders and clearly indicates degraded state. | P1 |  |  |  |
| app/dashboard/page.tsx | `/dashboard` | Dashboard overview loads | 1. Log in as a merchant user. 2. Navigate to `/dashboard`. | Dashboard page renders with merchant-specific overview content. | P0 |  |  |  |
| app/dashboard/onboarding/page.tsx | `/dashboard/onboarding` | Onboarding overview loads | 1. Log in as a merchant. 2. Navigate to `/dashboard/onboarding`. | Onboarding status and steps are displayed without errors. | P1 |  |  |  |
| app/dashboard/settings/page.tsx | `/dashboard/settings` | Settings hub loads | 1. Log in as a merchant. 2. Navigate to `/dashboard/settings`. | Settings overview page renders with navigation to sub-settings. | P1 |  |  |  |
| app/dashboard/settings/store/page.tsx | `/dashboard/settings/store` | Store settings page loads | 1. Log in as a merchant. 2. Navigate to `/dashboard/settings/store`. | Store settings form (branding, basic config) appears without errors. | P1 |  |  |  |
| app/dashboard/settings/domain/page.tsx | `/dashboard/settings/domain` | Domain settings page loads | 1. Log in as a merchant. 2. Navigate to `/dashboard/settings/domain`. | Domain configuration UI renders, showing current domain state. | P0 |  |  |  |
| app/dashboard/settings/bank/page.tsx | `/dashboard/settings/bank` | Bank settings page loads | 1. Log in as a merchant. 2. Navigate to `/dashboard/settings/bank`. | Bank account onboarding form UI is rendered. | P0 |  |  |  |
| app/dashboard/settings/invoice/page.tsx | `/dashboard/settings/invoice` | Invoice settings page loads | 1. Log in as a merchant. 2. Navigate to `/dashboard/settings/invoice`. | Invoice settings (prefix/series) UI renders without errors. | P1 |  |  |  |
| app/dashboard/settings/onboarding/page.tsx | `/dashboard/settings/onboarding` | Onboarding settings page loads | 1. Log in as a merchant. 2. Navigate to `/dashboard/settings/onboarding`. | Onboarding detail UI renders correctly. | P1 |  |  |  |
| app/dashboard/products/page.tsx | `/dashboard/products` | Products list displays | 1. Log in as a merchant. 2. Navigate to `/dashboard/products`. | Products list/table displays current products or empty state. | P0 |  |  |  |
| app/dashboard/products/new/page.tsx | `/dashboard/products/new` | New product form loads | 1. Log in as a merchant. 2. Navigate to `/dashboard/products/new`. | New product creation form is displayed. | P0 |  |  |  |
| app/dashboard/products/[id]/edit/page.tsx | `/dashboard/products/[id]/edit` | Edit product screen loads | 1. Log in as a merchant. 2. Navigate to an existing product edit URL. | Edit product form loads with existing product data populated. | P0 |  |  |  |
| app/dashboard/products/import/page.tsx | `/dashboard/products/import` | Product import page loads | 1. Log in as a merchant. 2. Navigate to `/dashboard/products/import`. | CSV upload UI, template download button, and preview area are visible. | P0 |  |  |  |
| app/dashboard/orders/page.tsx | `/dashboard/orders` | Orders list displays | 1. Log in as a merchant. 2. Navigate to `/dashboard/orders`. | Orders table or list renders with recent orders or empty state. | P0 |  |  |  |
| app/dashboard/orders/[orderId]/page.tsx | `/dashboard/orders/[orderId]` | Order detail page loads | 1. Log in as a merchant. 2. Navigate to a specific order detail route. | Order detail view renders with header and key order information. | P0 |  |  |  |
| app/dashboard/orders/[orderId]/invoice/page.tsx | `/dashboard/orders/[orderId]/invoice` | Order invoice view loads | 1. Log in as a merchant. 2. Navigate to `/dashboard/orders/{id}/invoice`. | Invoice view for the order renders (PDF link or on-page invoice). | P0 |  |  |  |
| app/dashboard/ledger/page.tsx | `/dashboard/ledger` | Ledger page loads | 1. Log in as a merchant. 2. Navigate to `/dashboard/ledger`. | Ledger entries or empty state appear without errors. | P0 |  |  |  |
| app/dashboard/payouts/page.tsx | `/dashboard/payouts` | Payouts page loads | 1. Log in as a merchant. 2. Navigate to `/dashboard/payouts`. | Payout overview (payout batches or summary) displays correctly. | P0 |  |  |  |
| app/dashboard/payouts/invoices/page.tsx | `/dashboard/payouts/invoices` | Payout invoices list loads | 1. Log in as a merchant. 2. Navigate to `/dashboard/payouts/invoices`. | List of platform invoices related to payouts is shown. | P0 |  |  |  |
| app/dashboard/payouts/invoices/[invoiceId]/page.tsx | `/dashboard/payouts/invoices/[invoiceId]` | Payout invoice detail loads | 1. Log in as a merchant. 2. Navigate to a payout invoice detail route. | Selected platform invoice details render correctly. | P0 |  |  |  |
| app/dashboard/billing/page.tsx | `/dashboard/billing` | Billing overview page loads | 1. Log in as a merchant. 2. Navigate to `/dashboard/billing`. | Billing summary UI (fees/plan info) renders without errors. | P0 |  |  |  |
| app/dashboard/analytics/page.tsx | `/dashboard/analytics` | Analytics dashboard loads | 1. Log in as a merchant with analytics access. 2. Navigate to `/dashboard/analytics`. | Analytics cards and tables (conversion, sales, top customers) display. | P0 |  |  |  |
| app/dashboard/storefront/page.tsx | `/dashboard/storefront` | Storefront builder page loads | 1. Log in as a merchant. 2. Navigate to `/dashboard/storefront`. | Storefront configuration/builder UI renders (theme/builder sections). | P1 |  |  |  |
| app/dashboard/account-hold/page.tsx | `/dashboard/account-hold` | Account hold banner displays | 1. Log in as a merchant whose account is on hold. 2. Navigate to `/dashboard/account-hold`. | Page shows a clear hold status banner and reason information. | P0 |  |  |  |
| app/dashboard/support/page.tsx | `/dashboard/support` | Support tickets list loads | 1. Log in as a merchant. 2. Navigate to `/dashboard/support`. | Support ticket list or empty state appears. | P1 |  |  |  |
| app/dashboard/support/new/page.tsx | `/dashboard/support/new` | Create ticket form loads | 1. Log in as a merchant. 2. Navigate to `/dashboard/support/new`. | Create support ticket form is displayed. | P1 |  |  |  |
| app/dashboard/support/[id]/page.tsx | `/dashboard/support/[id]` | Ticket detail screen loads | 1. Log in as a merchant. 2. Navigate to a specific ticket detail route. | Ticket thread and reply UI render as expected. | P1 |  |  |  |
| app/dashboard/marketing/coupons/page.tsx | `/dashboard/marketing/coupons` | Coupons list page loads | 1. Log in as a merchant with coupon feature. 2. Navigate to `/dashboard/marketing/coupons`. | Coupons list or empty state is shown without errors. | P1 |  |  |  |
| app/dashboard/marketing/coupons/new/page.tsx | `/dashboard/marketing/coupons/new` | New coupon form loads | 1. Log in as a merchant. 2. Navigate to `/dashboard/marketing/coupons/new`. | New coupon creation form is rendered. | P1 |  |  |  |
| app/dashboard/marketing/coupons/[id]/edit/page.tsx | `/dashboard/marketing/coupons/[id]/edit` | Edit coupon form loads | 1. Log in as a merchant. 2. Navigate to `/dashboard/marketing/coupons/{id}/edit`. | Existing coupon data is displayed in edit form. | P1 |  |  |  |
| app/dashboard/setup/page.tsx | `/dashboard/setup` | Setup wizard screen loads | 1. Log in as a new merchant. 2. Navigate to `/dashboard/setup`. | Setup/wizard UI renders guiding through initial configuration. | P1 |  |  |  |
| app/dashboard/settings/onboarding/page.tsx | `/dashboard/settings/onboarding` | Onboarding settings accessible | 1. Log in as a merchant. 2. Navigate to `/dashboard/settings/onboarding`. | Onboarding detail or step-by-step configuration appears. | P1 |  |  |  |
| app/s/[slug]/page.tsx | `/s/[slug]` | Storefront home loads | 1. Navigate to a valid store URL `/s/{slug}`. | Public storefront homepage renders products or hero content. | P0 |  |  |  |
| app/s/[slug]/p/[productId]/page.tsx | `/s/[slug]/p/[productId]` | Product detail screen loads | 1. Navigate to `/s/{slug}/p/{productId}`. | Product detail (name, price, image) is displayed. | P0 |  |  |  |
| app/s/[slug]/checkout/page.tsx | `/s/[slug]/checkout` | Checkout page loads | 1. Add product(s) to cart as a shopper. 2. Navigate to `/s/{slug}/checkout`. | Checkout form (address, payment options) renders without errors. | P0 |  |  |  |
| app/s/[slug]/order/[orderId]/page.tsx | `/s/[slug]/order/[orderId]` | Order status page loads | 1. Place an order. 2. Visit `/s/{slug}/order/{orderId}`. | Order summary and status are visible to the customer. | P0 |  |  |  |
| app/s/[slug]/order/[orderId]/payment/page.tsx | `/s/[slug]/order/[orderId]/payment` | Order payment page loads | 1. Place an order requiring payment. 2. Navigate to `/s/{slug}/order/{orderId}/payment`. | Payment continuation screen renders for the order. | P0 |  |  |  |
| app/admin/(protected)/page.tsx | `/admin` | Admin dashboard loads | 1. Log in as platform admin. 2. Navigate to `/admin`. | Admin dashboard home loads without errors. | P0 |  |  |  |
| app/admin/sign-in/page.tsx | `/admin/sign-in` | Admin sign-in page loads | 1. Navigate to `/admin/sign-in`. | Admin login UI renders for platform admins. | P0 |  |  |  |
| app/admin/(protected)/merchants/page.tsx | `/admin/merchants` | Admin merchant list loads | 1. Log in as platform admin. 2. Navigate to `/admin/merchants`. | Merchant list or empty state appears correctly. | P0 |  |  |  |
| app/admin/(protected)/merchants/[merchantId]/page.tsx | `/admin/merchants/[merchantId]` | Admin merchant detail screen loads | 1. Log in as platform admin. 2. Navigate to a specific merchant detail URL. | Merchant detail view loads with sections/tabs as defined in the UI. | P0 |  |  |  |
| app/admin/(protected)/merchants/[merchantId]/billing/page.tsx | `/admin/merchants/[merchantId]/billing` | Merchant billing tab loads | 1. Log in as platform admin. 2. Navigate to `/admin/merchants/{id}/billing`. | Billing overview for the merchant is rendered. | P0 |  |  |  |
| app/admin/(protected)/merchants/[merchantId]/pricing/page.tsx | `/admin/merchants/[merchantId]/pricing` | Merchant pricing tab loads | 1. Log in as platform admin. 2. Navigate to `/admin/merchants/{id}/pricing`. | Pricing package and overrides UI appear. | P0 |  |  |  |
| app/admin/(protected)/orders/page.tsx | `/admin/orders` | Admin orders list loads | 1. Log in as platform admin. 2. Navigate to `/admin/orders`. | Platform-wide orders list renders. | P0 |  |  |  |
| app/admin/(protected)/orders/[orderId]/page.tsx | `/admin/orders/[orderId]` | Admin order detail screen loads | 1. Log in as platform admin. 2. Navigate to `/admin/orders/{orderId}`. | Order detail information is visible with correct data. | P0 |  |  |  |
| app/admin/(protected)/payments/page.tsx | `/admin/payments` | Admin payments list loads | 1. Log in as platform admin. 2. Navigate to `/admin/payments`. | Payments table or list is shown with records. | P0 |  |  |  |
| app/admin/(protected)/payouts/page.tsx | `/admin/payouts` | Admin payouts list loads | 1. Log in as platform admin. 2. Navigate to `/admin/payouts`. | Payout batches or payouts table is displayed. | P0 |  |  |  |
| app/admin/(protected)/payouts/[payoutId]/page.tsx | `/admin/payouts/[payoutId]` | Admin payout detail screen loads | 1. Log in as platform admin. 2. Navigate to `/admin/payouts/{payoutId}`. | Detailed payout information is rendered. | P0 |  |  |  |
| app/admin/(protected)/platform-invoices/page.tsx | `/admin/platform-invoices` | Platform invoices list loads | 1. Log in as platform admin. 2. Navigate to `/admin/platform-invoices`. | List of all platform invoices renders correctly. | P0 |  |  |  |
| app/admin/(protected)/platform-invoices/[invoiceId]/page.tsx | `/admin/platform-invoices/[invoiceId]` | Platform invoice detail loads | 1. Log in as platform admin. 2. Navigate to `/admin/platform-invoices/{invoiceId}`. | Invoice detail page loads with line items. | P0 |  |  |  |
| app/admin/(protected)/pricing/page.tsx | `/admin/pricing` | Pricing overview loads | 1. Log in as platform admin. 2. Navigate to `/admin/pricing`. | Pricing packages summary appears. | P1 |  |  |  |
| app/admin/(protected)/pricing/new/page.tsx | `/admin/pricing/new` | New pricing package form loads | 1. Log in as platform admin. 2. Navigate to `/admin/pricing/new`. | New pricing package form is displayed. | P1 |  |  |  |
| app/admin/(protected)/pricing/[id]/page.tsx | `/admin/pricing/[id]` | Pricing package detail loads | 1. Log in as platform admin. 2. Navigate to `/admin/pricing/{id}`. | Selected pricing package detail view renders. | P1 |  |  |  |
| app/admin/(protected)/pricing/[id]/edit/page.tsx | `/admin/pricing/[id]/edit` | Edit pricing package screen loads | 1. Log in as platform admin. 2. Navigate to `/admin/pricing/{id}/edit`. | Editable pricing package form appears. | P1 |  |  |  |
| app/admin/(protected)/pricing-packages/page.tsx | `/admin/pricing-packages` | Pricing packages list loads | 1. Log in as platform admin. 2. Navigate to `/admin/pricing-packages`. | List of pricing packages loads. | P1 |  |  |  |
| app/admin/(protected)/pricing-packages/new/page.tsx | `/admin/pricing-packages/new` | New package form loads | 1. Log in as platform admin. 2. Navigate to `/admin/pricing-packages/new`. | Form to create a pricing package is visible. | P1 |  |  |  |
| app/admin/(protected)/pricing-packages/[id]/edit/page.tsx | `/admin/pricing-packages/[id]/edit` | Edit package screen loads | 1. Log in as platform admin. 2. Navigate to `/admin/pricing-packages/{id}/edit`. | Package editing UI renders with existing data. | P1 |  |  |  |
| app/admin/(protected)/dom
